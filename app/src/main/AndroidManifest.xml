<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tust.tmdbmovieviewer" >
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme"
        >
        <activity
            android:name="com.example.tust.tmdbmovieviewer.Activity.MainActivity"
            android:label="@string/app_name" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.example.tust.tmdbmovieviewer.Activity.PopularActivity"
            />

        <!-- Outbox Activity -->
        <activity android:name="com.example.tust.tmdbmovieviewer.Activity.TopRatedActivity" />

        <!-- Profile Activity -->
        <activity android:name="com.example.tust.tmdbmovieviewer.Activity.UpcomingActivity" />
        <activity android:name="com.example.tust.tmdbmovieviewer.Activity.SingleMovieView"  android:label="Single Item Selected">
        </activity>
    </application>

</manifest>

